import { Routes } from '@angular/router';
import { RecipeForm } from './features/recipes/pages/recipe-form/recipe-form';

export const routes: Routes = [
    {
        path: '',
        component: RecipeForm
    }
];
